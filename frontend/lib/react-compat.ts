/**
 * Legacy polyfill — kept for reference but superseded by the webpack alias in
 * next.config.js (lib/react-with-use-effect-event.cjs).
 * The webpack alias is more reliable because it patches the module before any
 * ESM named-import binding is established.
 */
export {};
